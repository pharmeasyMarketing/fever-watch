# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Secrets / env (commit only .env.example)
.env
*.env
!.env.example
serpapi.env
# Google Cloud service-account key for the Sheets API (live lab feed) - NEVER commit
secrets/
*.sa.json
gsheets_sa.json

# Internal docs and spreadsheets (never commit to a public repo)
*.xlsx
*.docx

# Lab-positivity raw counts + full lab city footprint are PROPRIETARY - never commit to the
# public repo (only the derived 0-100 signal in grid.json is public, by design). Keep only the
# counts-free runtime files the city resolver needs (raw -> config_id mapping + manual folds).
data/lab_feed_2025_historic.csv
data/positivity_detail.json
data/citymap/*
!data/citymap/city_alias_map.csv
!data/citymap/manual_aliases.csv
# Rain-provider benchmark CSVs (internal validation vs IMD gauge truth)
data/benchmark/

# Local data caches (committed data/*.json outputs are explicit and tracked)
data/cache/
# NOAA CPC yearly NetCDF rainfall files (regenerable; downloaded by providers/cpc.py)
data/cpc_cache/

# Historical weather backfill intermediates (regenerable from NASA POWER via
# build_weather.py --start/--end; the compact committed artifact is data/archive/)
data/backfill/

# SSG build output (regenerated by src/build_site.py)
dist/

# Generated per-city share cards (regenerated by src/build_share_cards.py from grid.json)
assets/img/og/
assets/img/share/
assets/img/push/

# Local scratch / one-off analysis artifacts at the repo root (not part of the build).
# _labs_2026_live_tmp.json is a raw live-lab dump - PROPRIETARY, must never be committed.
/_labs_2026_live_tmp.json
/Rainfall_ind2025_rfp25.grd
/crawl_fever-watch_homepage.html
/nasa_vs_era5_rainfall_benchmark_2026-06-15.csv
*.grd

# Local dev / analysis tooling - kept OUT of this public repo: it embeds proprietary lab counts
# and/or IMD non-commercial-licensed benchmark data (consistent with the data/citymap and
# data/benchmark ignores above). Lives only locally; back it up in a PRIVATE repo if wanted.
/scripts/citymap/
/scripts/agreement_gate_demo.py
/scripts/benchmark_2025_wet.py
/scripts/benchmark_chirps_cpc_228.py
/scripts/benchmark_weather_providers.py
/scripts/calibrate_sample_metros.py
/scripts/fill_blank_states.py
/scripts/gen_rain_doc.js
/scripts/imd_dsp_probe.py
/scripts/imd_metros_daily_csv.py
/scripts/imd_metros_table.py
/scripts/imd_vs_nasa_228.py
/scripts/probe_chirps_cpc.py
/scripts/production_window_table.py
/scripts/resolve_wikidata.py
/scripts/surgical_fix.py
/scripts/top20_all_sources.py
/prototypes/method-section-mock.html
/prototypes/method-section-*.html
/prototypes/method-options.html
/prototypes/national-map-mock.html
/_map_template.html
/_build_map_mock.py

# local page-audit scratch (not for the public repo)
/_audit_*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
